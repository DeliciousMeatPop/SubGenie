# Runtime dependencies for SubGenie.
# Everything else (hashing, parsing, config, embedding orchestration) uses only
# the Python standard library. ffmpeg is an external program, not a pip package.
requests>=2.25
